<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>כנס משרד הפנים 2026 - ממשק אינטראקטיבי</title>
    <!-- ייבוא גופנים ומערכות עזר -->
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;700;800;900&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        :root {
            --bg-dark: #020617;
            --accent-blue: #3b82f6;
            --color-policy: #a855f7;    /* פנים */
            --color-inno: #06b6d4;      /* רשויות */
            --color-social: #f97316;    /* מגזר שלישי */
            --color-res: #ef4444;       /* חירום וניהול משברים */
            --glass: rgba(15, 23, 42, 0.9);
            --border: rgba(255, 255, 255, 0.1);
        }

        body {
            font-family: 'Heebo', sans-serif;
            background-color: var(--bg-dark);
            color: white;
            margin: 0;
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        /* רקע חלקיקים */
        .bg-particles {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: -1; pointer-events: none;
        }
        .particle {
            position: absolute; background: var(--accent-blue);
            border-radius: 50%; opacity: 0.1; filter: blur(4px);
            animation: float-bg 25s infinite linear;
        }

        /* עיצוב המרחב המרכזי */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            perspective: 2000px;
            padding: 20px;
        }

        .agora-container {
            position: relative;
            width: 80vh;
            height: 80vh;
            max-width: 90vw;
            max-height: 90vw;
            display: flex;
            justify-content: center;
            align-items: center;
            transform-style: preserve-3d;
        }

        /* כפתור ספיד דייטינג מרכזי */
        .center-hub {
            position: absolute;
            width: 32%;
            height: 32%;
            background: radial-gradient(circle, #ffffff 0%, #3b82f6 30%, #1e3a8a 100%);
            border-radius: 50%;
            z-index: 200;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            box-shadow: 0 0 60px rgba(59, 130, 246, 0.8), inset 0 0 30px rgba(255,255,255,0.4);
            cursor: pointer;
            border: 4px solid white;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            text-align: center;
        }
        .center-hub:hover { transform: scale(1.05); box-shadow: 0 0 80px rgba(59, 130, 246, 1); }

        /* רבעים - טבעת רדיאלית */
        .quarter-slice {
            position: absolute;
            width: 48.5%; 
            height: 48.5%;
            cursor: pointer;
            transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
            backdrop-filter: blur(10px);
            z-index: 10;
            display: flex;
            justify-content: center;
            align-items: center;
            border: 1px solid rgba(255,255,255,0.1);
        }
        .quarter-slice:hover { z-index: 50; transform: scale(1.03) translateZ(20px); border-color: white; }
        .quarter-slice.active { z-index: 60; transform: scale(1.05) translateZ(40px); border-color: white; box-shadow: 0 0 50px currentColor; }

        .q-nw { top: 0; left: 0; border-radius: 100% 20px 20px 20px; background: linear-gradient(135deg, var(--color-policy) -30%, transparent 130%); }
        .q-ne { top: 0; right: 0; border-radius: 20px 100% 20px 20px; background: linear-gradient(225deg, var(--color-inno) -30%, transparent 130%); }
        .q-sw { bottom: 0; left: 0; border-radius: 20px 20px 20px 100%; background: linear-gradient(45deg, var(--color-res) -30%, transparent 130%); }
        .q-se { bottom: 0; right: 0; border-radius: 20px 20px 100% 20px; background: linear-gradient(315deg, var(--color-social) -30%, transparent 130%); }

        .quarter-content { display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; width: 100%; height: 100%; pointer-events: none; }
        .q-nw .quarter-content { transform: translate(15%, 15%); }
        .q-ne .quarter-content { transform: translate(-15%, 15%); }
        .q-sw .quarter-content { transform: translate(15%, -15%); }
        .q-se .quarter-content { transform: translate(-15%, -15%); }

        .q-icon { font-size: 3rem; margin-bottom: 5px; }
        .q-label { font-weight: 900; font-size: 1.2rem; color: white; line-height: 1.1; }

        /* HUD PANEL */
        .hud-panel {
            position: fixed;
            top: 50%; right: 20px;
            transform: translateY(-50%) translateX(130%);
            width: 400px;
            max-height: 85vh;
            background: var(--glass);
            border: 1px solid var(--border);
            border-top: 10px solid var(--accent-blue);
            padding: 30px;
            border-radius: 24px;
            backdrop-filter: blur(50px);
            transition: all 0.6s cubic-bezier(0.19, 1, 0.22, 1);
            box-shadow: 0 40px 100px rgba(0,0,0,0.8);
            z-index: 500;
            overflow-y: auto;
        }
        .hud-panel.active { transform: translateY(-50%) translateX(0); }

        /* POLL OVERLAY */
        .poll-overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(2, 6, 23, 0.98);
            z-index: 3000;
            display: none;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 30px;
        }
        .poll-overlay.active { display: flex; }

        /* AI CHAT */
        .ai-fab { position: fixed; bottom: 30px; left: 30px; width: 70px; height: 70px; background: linear-gradient(135deg, var(--accent-blue), #1e40af); border-radius: 50%; display: flex; justify-content: center; align-items: center; font-size: 2.5rem; cursor: pointer; z-index: 1000; border: 2px solid rgba(255,255,255,0.2); }
        .ai-window { position: fixed; bottom: 110px; left: 30px; width: 400px; height: 550px; background: #020617; border: 1px solid var(--accent-blue); border-radius: 24px; display: none; flex-direction: column; z-index: 999; box-shadow: 0 30px 100px rgba(0,0,0,0.9); }
        .ai-window.open { display: flex; }

        @keyframes float-bg { from { transform: translateY(100vh); } to { transform: translateY(-10vh); } }
    </style>
</head>
<body>

    <div class="bg-particles" id="particles"></div>

    <!-- ניווט עליון -->
    <nav class="fixed top-0 w-full z-[1000] p-4 flex justify-between items-center backdrop-blur-md border-b border-white/5">
        <div class="text-xl font-black text-white">Ministry of the Interior <span class="text-blue-500">2026</span></div>
        <button class="bg-red-600 px-5 py-1.5 rounded-full text-white text-xs font-bold shadow-lg" onclick="triggerPoll()">🚨 סקר בזק</button>
    </nav>

    <section class="hero">
        <div class="agora-container">
            <!-- רבע פנים -->
            <div class="quarter-slice q-nw" onclick="activate('policy')">
                <div class="quarter-content"><div class="q-icon">⚖️</div><div class="q-label">פנים</div></div>
            </div>
            <!-- רבע רשויות -->
            <div class="quarter-slice q-ne" onclick="activate('inno')">
                <div class="quarter-content"><div class="q-icon">🏢</div><div class="q-label">רשויות</div></div>
            </div>
            <!-- רבע חירום -->
            <div class="quarter-slice q-sw" onclick="activate('res')">
                <div class="quarter-content"><div class="q-icon">🚨</div><div class="q-label">חירום וניהול משברים</div></div>
            </div>
            <!-- רבע מגזר שלישי -->
            <div class="quarter-slice q-se" onclick="activate('social')">
                <div class="quarter-content"><div class="q-icon">🧡</div><div class="q-label">מגזר שלישי</div></div>
            </div>
            <!-- כפתור מרכזי -->
            <div class="center-hub" onclick="activate('core')">
                <div class="text-2xl mb-1">⏱️</div>
                <div class="font-black text-lg">ספיד דייטינג</div>
            </div>
        </div>
    </section>

    <!-- לוח מידע -->
    <div class="hud-panel" id="hud">
        <div onclick="closeHud()" style="position: absolute; top: 20px; left: 20px; cursor: pointer; opacity: 0.5;">✕</div>
        <h2 class="text-3xl font-black mb-4 text-white" id="hud-title">כותרת</h2>
        <p class="text-slate-300 text-base leading-relaxed mb-6" id="hud-desc"></p>
        <ul class="text-white text-sm space-y-3 list-disc list-inside bg-white/5 p-6 rounded-2xl border-r-4 border-blue-500" id="hud-bullets"></ul>
        <button class="w-full py-5 mt-8 bg-blue-600 rounded-2xl font-bold shadow-xl">✨ תובנה אסטרטגית (AI)</button>
    </div>

    <!-- סקר -->
    <div class="poll-overlay" id="pollOverlay">
        <div class="bg-slate-900 border-2 border-blue-500 p-10 rounded-[40px] text-center max-w-2xl w-full">
            <h2 class="text-4xl font-black mb-8" id="pollQuestion">שאלה אסטרטגית בטעינה...</h2>
            <div class="grid grid-cols-1 gap-4" id="pollOptions"></div>
            <button class="mt-8 opacity-50" onclick="closePoll()">סגור</button>
        </div>
    </div>

    <!-- AI FAB -->
    <div class="ai-fab" onclick="toggleChat()">✨</div>
    <div class="ai-window" id="chatWindow">
        <div class="p-4 border-b border-white/10 font-bold flex justify-between bg-blue-600/10 rounded-t-24">
            <span>יועץ AI - כנס משרד הפנים</span>
            <span onclick="toggleChat()" class="cursor-pointer">✕</span>
        </div>
        <div class="flex-1 p-4 overflow-y-auto space-y-4" id="chatContent"></div>
    </div>

    <script>
        const apiKey = ""; 
        const dataMap = {
            core: { 
                title: "ספיד דייטינג: הנהלת המשרד", 
                desc: "מתחם לקבלת החלטות מהירה ופתרון חסמים בזמן אמת מול בכירי המשרד.", 
                bullets: [
                    "5-10 עמדות פעילות לאורך כל שעות הכנס",
                    "מערכת דיגיטלית מקדימה לזימון פגישות מראש",
                    "מפגשים אישיים עם מנהלי אגפים וגורמי מקצוע בכירים",
                    "חתימה דיגיטלית וסיכומי החלטות מיידיים"
                ], color: "#3b82f6" 
            },
            policy: { 
                title: "רובע פנים: דוכני המשרד", 
                desc: "חשיפה מלאה לפעילות המנהלים והיחידות המקצועיות של משרד הפנים.", 
                bullets: [
                    "דוכני מידע אינטראקטיביים של כל יחידות המשרד",
                    "מפגשים בלתי אמצעיים עם המטה המקצועי",
                    "הצגת רפורמות מתוכננות בתכנון ומשילות",
                    "חשיפת כלים דיגיטליים לשירות הרשויות"
                ], color: "#a855f7" 
            },
            inno: { 
                title: "רובע רשויות: מיזמים עסקיים", 
                desc: "תערוכת הצלחות מוניציפליות המציגה מודלים כלכליים שיוצרים אימפקט.", 
                bullets: [
                    "כ-20 דוכני רשויות המציגות מודלים עסקיים מוצלחים",
                    "חלופה א': קול קורא להקמת דוכן במימון המשרד",
                    "חלופה ב': הזמנת רשויות פרטניות להציג הצלחות",
                    "למידת עמיתים על היתכנות כלכלית בשטח"
                ], color: "#06b6d4" 
            },
            res: { 
                title: "חירום וניהול משברים", 
                desc: "המרכז האופרטיבי המשלב גופי חירום וסימולציות ניהול אירוע.", 
                bullets: [
                    "הצגת גופי חירום וביטחון ציבוריים (משטרה, פקע״ר, כבאות)",
                    "סימולציות אינטראקטיביות לניהול אירועי קיצון",
                    "תרגול קבלת החלטות מנהיגותית תחת לחץ",
                    "חשיפת מערכות טכנולוגיות לשליטה ובקרה"
                ], color: "#ef4444" 
            },
            social: { 
                title: "רובע מגזר שלישי: שותפויות", 
                desc: "חשיפה לפעילות גופים גדולים במרחב לחיזוק שיתופי הפעולה והבנת המצב הנתון.", 
                bullets: [
                    "דוכנים לגופי מגזר שלישי מובילים (כגון: ג'וינט-אלכא, קרן רש\"י, ינאביע, מעוז)",
                    "הצגת מגוון המיזמים המבוצעים כיום בשטחי הרשויות",
                    "חשיפת הנהגת הרשויות למנעד הארגונים הפועלים בשטחן",
                    "מיפוי המצב הנתון וחיבור אסטרטגי למגזר הארגוני"
                ], color: "#f97316" 
            }
        };

        function activate(id) {
            document.querySelectorAll('.quarter-slice, .center-hub').forEach(el => el.classList.remove('active'));
            const selector = id === 'core' ? '.center-hub' : `.q-${id === 'policy' ? 'nw' : id === 'inno' ? 'ne' : id === 'social' ? 'se' : 'sw'}`;
            const target = document.querySelector(selector);
            if(target) target.classList.add('active');

            const hud = document.getElementById('hud');
            const data = dataMap[id];
            
            document.getElementById('hud-title').innerText = data.title;
            document.getElementById('hud-desc').innerText = data.desc;
            const bullets = document.getElementById('hud-bullets');
            bullets.innerHTML = data.bullets.map(b => `<li>${b}</li>`).join('');
            hud.style.borderTopColor = data.color;
            hud.classList.add('active');
        }

        function closeHud() { 
            document.getElementById('hud').classList.remove('active'); 
            document.querySelectorAll('.quarter-slice, .center-hub').forEach(el => el.classList.remove('active')); 
        }

        function triggerPoll() {
            document.getElementById('pollOverlay').classList.add('active');
            document.getElementById('pollQuestion').innerText = "מהו האתגר הבוער ביותר ל-2026?";
            const opts = ["רגולציה", "תקציב", "חוסן קהילתי", "מוכנות לחירום"];
            const container = document.getElementById('pollOptions');
            container.innerHTML = opts.map(opt => `<button class="p-6 bg-white/5 border border-white/10 rounded-xl hover:bg-blue-600 font-bold" onclick="closePoll()">${opt}</button>`).join('');
        }

        function closePoll() { document.getElementById('pollOverlay').classList.remove('active'); }
        function toggleChat() { document.getElementById('chatWindow').classList.toggle('open'); }

        // חלקיקים ברקע
        const particlesContainer = document.getElementById('particles');
        for (let i = 0; i < 20; i++) {
            const p = document.createElement('div');
            p.className = 'particle';
            const size = Math.random() * 80 + 20;
            p.style.width = size + 'px'; p.style.height = size + 'px';
            p.style.left = Math.random() * 100 + 'vw';
            p.style.top = Math.random() * 100 + 'vh';
            p.style.animationDelay = Math.random() * 15 + 's';
            particlesContainer.appendChild(p);
        }
    </script>
</body>
</html>
